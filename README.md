# TaskEvidence

Writes standardized task evidence JSON for automation plugins under `Saved/TaskEvidence/<Tool>/<RunId>/evidence.json`.
Use `-TaskEvidenceDir=...`, `-TaskEvidenceTaskId=...`, `-TaskEvidenceRunId=...`, or `-TaskEvidenceDisabled` to control command-line runs.